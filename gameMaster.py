import pygame

class GM():
    def __init__(self,pData):
        self.elements = [pData.handGroup, ]
        self.select = 0

    def selectMove(num):
    if len(pData.handGroup) <= 0:
        return
    pData.select += num
    pData.handGroup(select)